# Base URL of the running Monday-clone app (Docker maps container:3000 -> host:4000).
MONDAY_API_URL=http://localhost:4000

# Credentials for a user account the MCP server logs in as.
# Admin-only tools (boards/columns/groups/users) require an admin account.
MONDAY_EMAIL=admin@example.com
MONDAY_PASSWORD=change-me-admin
