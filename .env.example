DATABASE_URL="postgresql://monday:monday@localhost:5432/monday?schema=public"
APP_PASSWORD="change-me"
# Empty: APP_PASSWORD is treated as admin (backward compat). Set this to require
# a separate password for member management (create/edit/delete members).
ADMIN_PASSWORD=""
SESSION_SECRET="please-generate-a-long-random-string"
UPLOAD_DIR="/data/uploads"
MAX_UPLOAD_BYTES="10485760"
