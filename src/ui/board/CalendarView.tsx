"use client";
import type { BoardFull } from "./types";
export default function CalendarView({ board: _board }: { board: BoardFull }) {
  return <p style={{ color: "#888" }}>Calendar view — coming soon.</p>;
}
